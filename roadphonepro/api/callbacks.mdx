---
title: 'Server Callbacks'
description: 'All Bridge callbacks registered by RoadPhone Pro (client → server, with return value)'
icon: 'arrow-right-left'
---

import ApiCallbacksRadio from '/snippets/api/apicallbacks-radio.mdx';
import ApiCallbacksHousing from '/snippets/api/apicallbacks-housing.mdx';
import ApiCallbacksDarkchat from '/snippets/api/apicallbacks-darkchat.mdx';

# Server Callbacks

Callbacks are request/response calls from the client to the server — unlike [Server Events](/roadphonepro/api/serverevents), they **return a value**. RoadPhone registers all of them through the Bridge system:

```lua
-- Server: register
Bridge.RegisterCallback('name', function(source, cb, arg1, arg2)
    cb(result)
end)

-- Client: trigger
Bridge.TriggerCallback('name', function(result)
    print(result)
end, arg1, arg2)
```

<Note>
  The Bridge maps callbacks onto the active framework: `ESX.TriggerServerCallback`, `QBCore.Functions.TriggerCallback`, `lib.callback` (Qbox) or an internal net-event protocol (standalone, with a 10s timeout). Because of that, other resources can call these callbacks through their framework's native callback mechanism as well.

  Most callbacks return **one** value; a few return two (e.g. the backup callbacks and `roadphone:mechanic:resetPin` call `cb(success, resultOrError)`) — your handler function then receives two arguments.
</Note>

<Warning>
  As with events, identity is server-authoritative: callbacks resolve the caller's phone number, identifier, job and account sessions from `source`. Client-supplied account IDs are ignored.
</Warning>

<Note>
  App names and internal identifiers differ on purpose: Connect events still carry the `instagram` prefix, Glimpse uses `snapy`, TweetWave uses `twitter`. Renaming them would break every existing install, so the identifiers stay as they are.
</Note>

---

## Core Phone

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:getItemAmount` | — | `{ item, metadata }` or `nil` | **The primary phone-open callback.** Verifies the phone item and returns the full per-phone metadata bundle (number, contacts, messages, notes, settings, PIN, alarms, accounts, photos, SIMs, battery, …) used to hydrate the UI. |
| `roadphone:hasPhoneItem` | — | boolean | Does the caller carry a configured phone item? Used before opening the phone. |
| `roadphone:switchToPhoneByNumber` | `phoneNumber` | `{ metadata }` or `nil` | Switches the active phone item to the one holding that number (used before an incoming call rings). Metadata mode only; phone must belong to the caller. |
| `roadphone:getPhoneNumber` | — | `number (string)` | Returns (and creates if needed) the active phone's number. |
| `roadphone:getPhonePin` | — | `{ pin, pin_needed }` or `nil` | Returns the stored PIN state (metadata mode only). |
| `roadphone:server:getContacts` | — | array of contact rows | The caller's contacts (max 500). |
| `roadphone:getPictures` | — | array of photo rows or `nil` | Gallery photos of the current phone (metadata or DB). |
| `roadphone:server:getOfflineNotifys` | — | array of notification rows | Fetches **and deletes** queued offline notifications (drain semantics). |
| `roadphone:service:getJobCallMembers` | `number` | array of `{ source, name }` | Online dispatchers registered for a service/emergency number. |
| `roadphone:server:gfu` | `type?` | presigned-upload object or `nil` | Server-side proxy for fmapi.net presigned upload URLs (API key stays on the server). |
| `roadphone:server:custom:upload:request` | `filetype?` | presigned-upload object or `nil` | Same for a self-hosted upload endpoint (`uploader/upload-server.lua` template). |
| `roadphone:checkUpdate` | — | `{ update, version, changelog, newVersion, isBeta }` | Cached version-check info. |
| `roadphone:weather:getTemp` | `weatherType` | temperature string (e.g. `"21°C"`) | Authoritative, cached temperature so all clients agree. |
| `roadphone:checkIsBoss` | — | boolean | Whether the caller is a boss of their job. |
| `roadphone:server:getEmployees` | `society` | array of `{ firstname, lastname, job_grade, phone_number, online }` | Employees of a job with online status (30s cache). |

<ApiCallbacksRadio />

## Bank & Billing

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:getBankData` | — | `{ balance, iban, transactions, requests, ibanPrefix?, ibanLength? }` | Aggregated bank snapshot incl. pending payment requests. |
| `roadphone:getBankBalance` | — | number | Legacy: just the bank balance. |
| `roadphone:bank:checkSpam` | — | `1` (spam) or `0` | Per-player rate-limit gate (1s). |
| `roadphone:bank:createRequest` | `targetNumber, amount, reason` | `{ ok }` or `{ ok = false, error }` | Creates a money request (max 3 pending per pair, 24h expiry). |
| `roadphone:bank:respondRequest` | `requestId, accept` | `{ ok }` or `{ ok = false, error }` | Accepts (executes transfer) or declines a request (recipient-only, race-safe). |
| `roadphone:okokBanking:getIban` | — | IBAN string | Legacy: okokBanking IBAN (now part of `getBankData`). |
| `roadphone:rxBanking:getCreditCard` | — | card number string | Legacy: RX Banking card number. |
| `roadphone:getbilling` | — | array of `{ id, label, sender, target, amount }` | The caller's bills. Registered in one of two variants depending on `Config.bcsCompanyManager`. |
| `roadphone:getRxBilling` | — | array of invoice objects | Normalized RxBilling invoices. |
| `roadphone:getRxBillingHistory` | — | array of invoice objects | Paid/archived RxBilling invoices. |
| `roadphone:getQfBilling` | — | array of invoice objects | Open qb-management invoices (`unpaid` only — `request`, `rejected` and `canceled` are workflow states, not bills). |
| `roadphone:getQfBillingHistory` | — | array of invoice objects | Paid qb-management invoices. |
| `roadphone:getBillingHistory` | — | array of invoice objects | Paid bills of the active billing system. |
| `roadphone:getCodeMBilling2` | — | codem unpaid-billing list | Passthrough for codem-billing2. |

<ApiCallbacksHousing />

## Service (Dispatch & Business)

The `management*` callbacks are rate-limited and require management permission (job + grade).

| Callback | Input | Returns | Description |
|---|---|---|---|
| `getBusinessStatus` | — | map of job → status object | Open/closed state + employee counts for all businesses. |
| `getAllBusinessNews` | — | array of news rows | Global business-news feed. |
| `getBusinessNews` | `job` | array of news rows | News for one business. |
| `getOnlineCoworkers` | — | array of `{ source, name, grade }` | Online players sharing the caller's job. |
| `getActiveDispatches` | — | array of dispatch rows | Open dispatches for the caller's job. |
| `getDispatchHistory` | — | array of dispatch rows | Finished/declined dispatches. |
| `getMySentDispatches` | — | array of dispatch rows | Dispatches this player sent themselves. |
| `getDispatchStats` | — | `{ today, completed, declined }` | Today's dispatch counts. |
| `getManagementData` | — | `{ success, balance, employees }` | Society balance + full employee roster. |
| `managementDeposit` | `amount` | `{ success, newBalance }` or error | Deposits into the society account (refund-safe). |
| `managementWithdraw` | `amount` | `{ success, newBalance }` or error | Withdraws from the society account (atomic). |
| `managementHire` | `targetServerId` | `{ success }` or error | Hires an online player at grade 0. |
| `managementFire` | `targetIdentifier` | `{ success }` or error | Fires an employee (online or offline). |
| `managementSetGrade` | `targetIdentifier, newGrade` | `{ success }` or error | Changes a grade (cannot exceed own grade). |

## Taxi / Calendar / Notes / Reminders / Voice Memos

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:taxi:getDriverStatus` | — | boolean | Whether the caller may drive a taxi (job/license config). |
| `roadphone:taxi:gethistory` | — | array of trip rows | Past rides (max 50). |
| `roadphone:taxi:getfavorites` | — | array of `{ type, name, coords }` | Saved favourite destinations. |
| `roadphone:calendar:getEvents` | — | array of event rows | Calendar events (max 300). |
| `roadphone:notes:getNotes` | — | array of note rows | Notes (max 300). |
| `roadphone:reminders:getAll` | — | `{ lists, reminders }` | All reminder lists and their entries for the caller. |
| `roadphone:voicememos:get` | — | array of memo rows | Saved voice memos. |

## Music / Crypto / Health / RoadDrop

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:music:loadPlaylists` | — | `{ only, songs }` | Playlists + their songs. |
| `roadphone:music:getSearch` | `search` | array of track objects | Library search (or external Music API). |
| `roadphone:music:isMusicAdmin` | — | boolean | Music-admin status. |
| `roadphone:music:getSubmissions` | — | array of pending submissions | Admin-only review queue. |
| `roadphone:music:getLibraryAdmin` | — | array of library songs | Admin-only full library. |
| `roadphone:crypto:getWallet` | — | wallet address string or `nil` | The caller's wallet address, created on first use. |
| `roadphone:crypto:getCrypto` | — | map `coinid → amount` | The caller's crypto holdings. |
| `roadphone:crypto:checkSpam` | — | `1` or `0` | Anti-spam gate for crypto actions. |
| `roadphone:health:getData` | — | `{ today, history, current }` | Today's totals, 7-day history, live vitals. |
| `roadphone:roaddrop:getAccountInformation` | `playerIds` | array of `{ firstname, lastname, profile, playerId }` | Resolves display names of nearby players. |

<ApiCallbacksDarkchat />

## Glimpse

All resolve identity via the server session; the NUI-supplied `accountId` is always overridden.

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:snapy:resume` | — | account row or `false` | Auto-login resume. |
| `roadphone:snapy:getFriends` | — | array of friend rows | Accepted friends. |
| `roadphone:snapy:getPendingRequests` | — | array of request rows | Incoming pending requests. |
| `roadphone:snapy:getBlocked` | — | array of blocked users | Blocked list. |
| `roadphone:snapy:getSuggestions` | — | array of suggestions (max 6) | Quick-add: friends-of-friends. |
| `roadphone:snapy:getNearby` | `serverIds` | array of nearby users, or `false` | Nearby Snapy users (distance + privacy verified, rate-limited). |
| `roadphone:snapy:getReceivedSnaps` | — | array of snap rows | Unviewed received snaps. |
| `roadphone:snapy:getSentSnaps` | `friendId` | array of snap rows | Snaps sent to a friend. |
| `roadphone:snapy:replaySnap` | `snapId` | snap row or `false` | One-time replay (recipient-only, `mode = 'replay'`). |
| `roadphone:snapy:getStories` | — | array of story rows | Visible active stories (visibility-filtered). |
| `roadphone:snapy:getStoryViewers` | `storyId` | array of viewer rows | Story viewers (author-only). |
| `roadphone:snapy:getHighlights` | — | array of stories | The caller's profile highlights. |
| `roadphone:snapy:getChats` | — | array of chat partners | Chat list with streaks, unread counts, friend emoji. |
| `roadphone:snapy:getFriendship` | `friendId` | friendship object or `false` | Friendship details (streak, mutuals, emoji). |
| `roadphone:snapy:getChatMessages` | `friendId` | array of messages (≤100) | 1:1 chat history (marks incoming read). |
| `roadphone:snapy:searchUsers` | `searchTerm` | array of users (max 20) | User search (min 2 chars, rate-limited). |
| `roadphone:snapy:createGroup` | `{ name, members }` | group object or `false` | Creates a group (members must be friends, max 32). |
| `roadphone:snapy:getGroups` | — | array of group rows | The caller's groups with unread counts. |
| `roadphone:snapy:getGroupMessages` | `groupId` | array of messages (≤100) | Group chat history (member-only, advances read pointer). |
| `roadphone:snapy:getMemories` | — | array of memory rows | The memories vault. |

## Loop

The short-video app. Gated by `Config.Loop`; every callback resolves the caller's Loop account from the server-side session — a client-supplied account ID is never trusted.

### Account & profile

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:loop:register` | `handle, password, displayName` | `{ ok }` or `{ ok = false, error }` | Creates a Loop account and opens a session. |
| `roadphone:loop:login` | `handle, password` | `{ ok, account }` or error | Logs in and mints the session on the phone. |
| `roadphone:loop:logout` | — | `{ ok }` | Ends the session for this phone. |
| `roadphone:loop:resume` | — | account object or `nil` | Resumes the stored session when the app opens. |
| `roadphone:loop:getProfile` | `handleOrId` | profile object | Public profile with counters and relation state. |
| `roadphone:loop:updateProfile` | `patch` | `{ ok }` or error | Display name, bio, avatar, privacy flags. |
| `roadphone:loop:getPermissions` | — | permission flags | What this account may do (creator tools, moderation). |
| `roadphone:loop:linkSocial` | `kind, unlink` | `{ ok }` | Links or unlinks a Connect / TweetWave / Snapy handle. |
| `roadphone:loop:getStudio` | — | creator dashboard | Analytics overview for the account's own posts. |

### Feed & posts

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:loop:getFeed` | `kind, cursor` | `{ posts, nextCursor }` | Feed page — `kind` selects For You, Following, Nearby … |
| `roadphone:loop:getPost` | `postId` | post object | Single post incl. author, sound and counters. |
| `roadphone:loop:createPost` | `payload` | `{ ok, postId }` or error | Publishes a post (video/photo, caption, sound, privacy). |
| `roadphone:loop:deletePost` | `postId` | `{ ok }` | Deletes an own post. |
| `roadphone:loop:saveDraft` | `payload` | `{ ok, draftId }` | Stores a draft server-side. |
| `roadphone:loop:getDrafts` | — | array of drafts | The caller's drafts. |
| `roadphone:loop:deleteDraft` | `draftId` | `{ ok }` | Deletes a draft. |
| `roadphone:loop:getUserPosts` | `ownerId, tab, offset` | array of posts | Profile tabs: posts, reposts, liked, saved. |
| `roadphone:loop:recordViews` | `batch` | `{ ok }` | Batched view counting for the posts just watched. |
| `roadphone:loop:getPostStats` | `postId` | stats object | Views, likes, shares, watch-through for one post. |
| `roadphone:loop:pushPreview` | `ref` | `{ ok }` | Pushes a preview of a post to the phone (share flow). |

### Reactions & comments

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:loop:like` | `postId, liked` | `{ ok, likes }` | Likes / unlikes a post. |
| `roadphone:loop:toggleSave` | `postId, wantSave` | `{ ok }` | Saves a post to the account's collection. |
| `roadphone:loop:repost` | `postId, wantRepost` | `{ ok }` | Reposts or takes the repost back. |
| `roadphone:loop:notInterested` | `postId, scope` | `{ ok }` | Feedback signal — `scope` hides the post, the author or the sound. |
| `roadphone:loop:getComments` | `postId, offset` | array of comments | Paged comments, newest first. |
| `roadphone:loop:getReplies` | `parentId, offset` | array of comments | Replies to one comment. |
| `roadphone:loop:addComment` | `postId, text, parentId` | `{ ok, comment }` | Comments, or replies when `parentId` is set. |
| `roadphone:loop:editComment` | `commentId, text` | `{ ok }` | Edits an own comment. |
| `roadphone:loop:deleteComment` | `commentId` | `{ ok }` | Deletes an own comment (or any, as post author). |
| `roadphone:loop:likeComment` | `commentId, liked` | `{ ok, likes }` | Likes a comment. |
| `roadphone:loop:pinComment` | `commentId, pinned` | `{ ok }` | Post author pins a comment to the top. |

### Social graph & messages

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:loop:follow` | `targetId, wantFollow` | `{ ok, state }` | Follows / unfollows; a private account gets a request instead. |
| `roadphone:loop:respondFollow` | `requesterId, accept` | `{ ok }` | Accepts or rejects a follow request. |
| `roadphone:loop:getRelations` | `ownerId, kind` | array of accounts | Followers, following or friends of an account. |
| `roadphone:loop:block` | `targetId, wantBlock` | `{ ok }` | Blocks or unblocks an account. |
| `roadphone:loop:getThreads` | — | array of threads | DM overview with last message and unread count. |
| `roadphone:loop:getMessages` | `threadId, beforeId` | array of messages | Paged messages of one thread. |
| `roadphone:loop:sendDm` | `targetId, payload` | `{ ok, message }` | Sends a DM — text, post share or sound. |
| `roadphone:loop:markThreadRead` | `threadId` | `{ ok }` | Marks a thread as read. |
| `roadphone:loop:getInbox` | `tab, offset` | array of notifications | Activity inbox per tab (all, likes, comments, follows). |
| `roadphone:loop:markInboxRead` | `notificationId` | `{ ok }` | Marks one notification — or all — as read. |
| `roadphone:loop:getUnread` | — | `{ inbox, dm }` | Badge counters for the app icon. |

### Sounds & collections

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:loop:getSound` | `soundId` | sound object | One sound with its metadata. |
| `roadphone:loop:getSoundPosts` | `soundId, offset` | array of posts | Everything made with that sound. |
| `roadphone:loop:getTrendingSounds` | — | array of sounds | Currently trending sounds. |
| `roadphone:loop:getSoundLibrary` | `query` | array of tracks | Searches the server's music library for a sound. |
| `roadphone:loop:useLibrarySound` | `track` | `{ ok, soundId }` | Turns a library track into a usable Loop sound. |
| `roadphone:loop:getCollections` | — | array of collections | The account's playlists/collections. |
| `roadphone:loop:createPlaylist` | `name` | `{ ok, collectionId }` | Creates a collection. |
| `roadphone:loop:getPlaylist` | `collectionId` | collection with posts | One collection. |
| `roadphone:loop:addToPlaylist` | `collectionId, postId, wantAdd` | `{ ok }` | Adds or removes a post. |

### Discovery & search

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:loop:getDiscover` | — | `{ tags, sounds, creators }` | Discover page content. |
| `roadphone:loop:search` | `query, kind` | array of results | Searches posts, accounts, sounds or tags. |
| `roadphone:loop:getTag` | `tag, offset` | array of posts | Hashtag page. |
| `roadphone:loop:lookup` | `kind, value` | lookup result | Resolves a phone number or handle to a Loop account. |
| `roadphone:loop:getLookupLog` | — | array of lookups | Who looked this account up — the app's own privacy log. |

### Live

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:loop:getLives` | — | array of live streams | Currently running streams. |
| `roadphone:loop:startLive` | `title` | `{ ok, liveId }` | Starts a stream. |
| `roadphone:loop:endLive` | — | `{ ok }` | Ends the caller's stream. |
| `roadphone:loop:joinLive` | `liveId` | `{ ok, live }` | Joins as a viewer. |
| `roadphone:loop:leaveLive` | `liveId` | `{ ok }` | Leaves the stream. |
| `roadphone:loop:liveChat` | `liveId, text` | `{ ok }` | Sends a chat message into the stream. |
| `roadphone:loop:sendLiveGift` | `liveId, giftKey` | `{ ok, balance }` | Sends a gift to the streamer. |
| `roadphone:loop:challengeDuel` | `targetLiveId` | `{ ok, duelId }` | Challenges another stream to a duel. |
| `roadphone:loop:respondDuel` | `duelId, accept` | `{ ok }` | Accepts or declines a duel. |

### Economy

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:loop:getWallet` | — | `{ coins, diamonds }` | The account's balances. |
| `roadphone:loop:buyCoins` | `packIndex` | `{ ok, coins }` | Buys a coin pack with in-game money. |
| `roadphone:loop:sendGift` | `postId, giftKey` | `{ ok, balance }` | Gifts a post; the creator earns diamonds. |
| `roadphone:loop:getPostGifts` | `postId` | array of gifts | Who gifted what on a post. |
| `roadphone:loop:payout` | `diamonds` | `{ ok, amount }` | Converts diamonds into in-game money. |
| `roadphone:loop:promote` | `postId, hours` | `{ ok }` | Pays to boost a post's reach for a while. |

### Moderation

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:loop:report` | `targetKind, targetId, reason, note` | `{ ok }` | Reports a post, comment or account. |
| `roadphone:loop:getReports` | `state` | array of reports | Moderator queue by state. |
| `roadphone:loop:handleReport` | `reportId, action` | `{ ok }` | Resolves a report (dismiss, remove, ban). |
| `roadphone:loop:setAccountFlag` | `accountId, flag, value` | `{ ok }` | Sets moderation flags such as verified or shadowban. |

<Note>
  Moderation callbacks require the moderator permission from `roadphone:loop:getPermissions` — they are not open to every account.
</Note>

## Wallet & Events

Wallet callbacks verify distance and identity server-side; the Events callbacks feed the tickets and membership cards the Wallet shows. Failure reasons and payloads are on the [Wallet API](/roadphonepro/api/wallet) page.

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:wallet:getCards` | — | `{ ok, cards, profile }` | Every card of the caller, built live. |
| `roadphone:wallet:setPhoto` | `url` | `{ ok, photo }` | Stores the ID photo URL (upload host allow-list, 1 per 3 s). |
| `roadphone:wallet:getNearby` | `serverIds` | array of `{ playerId, name, distance }` | Players within `ShowRadius + 2 m`, same routing bucket. |
| `roadphone:wallet:show` | `cardId, playerId` | `{ ok }` | Shows a card to one nearby player (1 per 2 s). |
| `roadphone:wallet:checkIn` | `code` | `{ ok, checkedIn }` | Event owner checks a shown ticket in. |
| `roadphone:events:list` | — | `{ ok, events, isBoss, canCreate, limits }` | Upcoming events and the caller's permissions. |
| `roadphone:events:mine` | — | `{ ok, events }` | Own events and events with tickets. |
| `roadphone:events:get` | `id` | `{ ok, event, maxTicketsPerBuyer }` | One event. |
| `roadphone:events:create` | `data` | `{ ok, id }` | Creates an event within `Config.Events` limits. |
| `roadphone:events:buy` | `id, qty` | `{ ok, … }` | Buys tickets (bank money, `EV-XXXXXX` codes). |
| `roadphone:events:cancel` | `id` | `{ ok }` | Cancels and marks tickets for refund. |
| `roadphone:events:end` | `id` | `{ ok }` | Ends an event early. |
| `roadphone:events:sales` | `id` | `{ ok, tickets, sold, revenue, checkedIn, pendingPayout }` | Organizer overview. |
| `roadphone:events:collect` | — | `{ ok, … }` | Pays out pending money. |
| `roadphone:events:members` | — | `{ ok, job, members }` | Membership cards of the boss's job. |
| `roadphone:events:issueCard` | `playerId, label, color, days` | `{ ok, id }` | Issues a membership card to a nearby player. |
| `roadphone:events:revokeCard` | `id` | `{ ok }` | Revokes a membership card. |

## Find My

Location sharing between phone numbers, gated by `Config.FindMy`. Shares expire on their own; a permanent share is one without a duration.

| Callback | Input | Returns | Description |
|---|---|---|---|
| `roadphone:findmy:getData` | — | `{ myNumber, people, myShares, devices }` or `nil` | Full app state: who shares with me, who I share with, my devices. |
| `roadphone:findmy:getPositions` | — | `{ people, devices }` or `nil` | Poll for live positions — online sharers by ped coords, offline ones by last seen. |
| `roadphone:findmy:share` | `targetNumber, durationSeconds` | `{ ok }` | Starts or refreshes a share. No duration = permanent, a duration = temporary live location. |
| `roadphone:findmy:revoke` | `targetNumber` | `{ ok }` | Stops sharing with that number. |
| `roadphone:findmy:hideFrom` | `ownerNumber` | `{ ok }` | Hides someone else's share from your map without revoking theirs. |

## Addons

| Callback | Addon | Input | Returns | Description |
|---|---|---|---|---|
| `roadphone:simcard:getSIMs` | SIM Card | — | `{ sims, activeSim }` | Installed SIMs + active SIM. |
| `roadphone:simcard:getInventorySIMs` | SIM Card | — | array of SIM items | SIM cards in the inventory. |
| `roadphone:simcard:insertSIM` | SIM Card | `simSlot, simId` | `{ success, sims, activeSim }` or error | Inserts a SIM. |
| `roadphone:simcard:ejectSIM` | SIM Card | `simIndex` | `{ success, sims, activeSim }` or error | Ejects a SIM. |
| `roadphone:simcard:switchSIM` | SIM Card | `simIndex` | `{ success, newNumber, credits }` or error | Switches the active SIM (re-caches numbers). |
| `roadphone:simcard:convertESIM` | SIM Card | `simIndex` | `{ success, sims, activeSim }` or error | Converts a SIM to eSIM. |
| `roadphone:simcard:buySIM` | SIM Card | — | `{ success, number }` or error | Buys a SIM (charges `SimCard.SIMPrice`, refund-safe). |
| `roadphone:simcard:topUp` | SIM Card | `packageId` | `{ success, credits }` or error | Tops up credit (refund-safe). |
| `roadpods:checkItemAmount` | RoadPods | — | item name, `true`, or `nil` | Verifies RoadPods ownership. |
| `roadphone:mechanic:resetPin` | Mechanic | — | `success, amountOrError` (two values) | Resets the phone PIN at the mechanic NPC (optional payment, refund-safe). |
